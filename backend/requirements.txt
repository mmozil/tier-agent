# Web framework
fastapi==0.118.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Cache / queue
redis==5.2.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
email-validator==2.2.0
cryptography==44.0.0
google-auth==2.36.0

# HTTP
httpx==0.28.1

# Container orchestration (SSH para host Docker daemon)
docker==7.1.0
paramiko==3.5.0

# LLM providers (apenas SDK leve — agent_runtime usa via REST)
# Pins frouxos pra evitar release-cadence breaks
google-genai>=0.8.0,<2
anthropic>=0.40,<1
openai>=1.50,<2

# Storage
boto3==1.35.78

# Utils
python-multipart==0.0.20
python-dotenv==1.0.1
tenacity==9.0.0
apscheduler==3.11.0
croniter==3.0.4

# Doc processing (Fase 5 — knowledge upload)
pdfplumber==0.11.4
openpyxl==3.1.5

# Dev
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
